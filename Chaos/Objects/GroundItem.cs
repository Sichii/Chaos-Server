namespace Chaos.Objects
{
    internal sealed class GroundItem : VisibleObject
    {
        internal GroundItem(ushort sprite, Point point, Map map)
          : base(string.Empty, sprite, point, map)
        {
        }
    }
}
